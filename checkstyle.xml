<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">
<module name="Checker">
    <property name="charset" value="UTF-8"/>
    <property name="severity" value="error"/>
    <property name="fileExtensions" value="java"/>

    <!-- No tab characters anywhere in source files -->
    <module name="FileTabCharacter">
        <property name="eachLine" value="true"/>
    </module>

    <!-- Every file must end with a newline -->
    <module name="NewlineAtEndOfFile"/>

    <!-- Max line length; package/import and bare URL lines are exempt -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern"
                  value="^(package|import)\s|^\s*\*?\s*(https?|ftp)://\S"/>
    </module>

    <!--
        Suppress MethodName for test classes only.
        Test methods intentionally use underscore-separated given_when_then names.
    -->
    <module name="SuppressionSingleFilter">
        <property name="checks" value="MethodName"/>
        <property name="files" value="[\\/]test[\\/].*\.java$"/>
    </module>

    <module name="TreeWalker">

        <!-- ── Naming ────────────────────────────────────────────── -->
        <!--
            Allow 'log' alongside SCREAMING_SNAKE_CASE.
            SLF4J loggers are universally declared as 'private static final Logger log'.
        -->
        <module name="ConstantName">
            <property name="format" value="^log$|^[A-Z][A-Z0-9]*(_[A-Z0-9]+)*$"/>
        </module>
        <module name="LocalVariableName"/>
        <module name="MemberName"/>
        <module name="MethodName"/>
        <module name="PackageName"/>
        <module name="ParameterName"/>
        <module name="StaticVariableName"/>
        <module name="TypeName"/>

        <!-- ── Imports ───────────────────────────────────────────── -->
        <module name="AvoidStarImport"/>
        <module name="IllegalImport"/>
        <module name="RedundantImport"/>
        <module name="UnusedImports"/>

        <!-- ── Braces ────────────────────────────────────────────── -->
        <module name="NeedBraces"/>
        <module name="LeftCurly"/>
        <module name="RightCurly"/>
        <module name="EmptyBlock"/>

        <!-- ── Modifiers ─────────────────────────────────────────── -->
        <module name="ModifierOrder"/>

        <!-- ── Miscellaneous ─────────────────────────────────────── -->
        <!-- Use L not l for long literals (e.g. 100L not 100l) -->
        <module name="UpperEll"/>
        <!-- Enforce T[] arr not T arr[] -->
        <module name="ArrayTypeStyle"/>

    </module>
</module>
